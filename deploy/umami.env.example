# DO NOT COMMIT deploy/umami.env (it contains secrets)

# Postgres password for Umami
UMAMI_DB_PASSWORD=CHANGE_ME

# Umami app secret (generate with: openssl rand -hex 32)
UMAMI_APP_SECRET=CHANGE_ME

# Where to expose Umami (LAN-only is fine for now)
UMAMI_BIND=0.0.0.0
UMAMI_PORT=3000
