# Legacy

This directory contains deprecated / historical pieces kept for reference.

- `legacy/server.py`: the old single-file backend (superseded by `backend/` FastAPI + Postgres).
- `legacy/remark42/`: the old Remark42 compose/env examples.

